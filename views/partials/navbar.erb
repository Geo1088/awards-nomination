<nav class="navbar is-dark">
  <div class="container">
    <div class="navbar-brand">
      <a href="/" class="navbar-item has-text-weight-semibold">/r/anime Awards 2018</a>
    </div>
    <div class="navbar-menu is-active">
      <div class="navbar-start">
        <% if @r %>
          <a href="/genres" class="navbar-item <%= @route == "genres" ? "is-active" : "" %>">Genres</a>
          <a href="/characters" class="navbar-item <%= @route == "characters" ? "is-active" : "" %>">Characters</a>
          <a href="/production" class="navbar-item <%= @route == "production" ? "is-active" : "" %>">Production</a>
          <a href="/main" class="navbar-item <%= @route == "main" ? "is-active" : "" %>">Main</a>
        <% end %>
      </div>
      <div class="navbar-end">
        <% if @r %>
          <div class="navbar-item has-dropdown is-hoverable is-right">
            <a class="navbar-link" href="#">
              <% if @r.me.subreddit %>
                <img class="avatar" src="<%= @r.me.subreddit[:icon_img] %>" alt="avatar">
              <% end %>
              <%= @r.me.name %>
            </a>
            <div class="navbar-dropdown">
              <a href="/logout" class="navbar-item">Log out</a>
            </div>
          </div>
        <% else %>
          <div class="navbar-item">
            <a class="button is-primary" href="/login">Sign in with Reddit</a>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>
